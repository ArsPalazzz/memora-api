{
    "type": "object",
    "required": ["cards_per_session", "card_orientation"],
    "additionalProperties": false,
    "properties": {
        "cards_per_session": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200
        },
        "card_orientation": {
            "type": "string",
            "enum": ["normal", "reversed", "mixed"]
        }
    }
}

{
    "type": "object",
    "required": ["card_orientation"],
    "additionalProperties": false,
    "properties": {
        "card_orientation": {
            "type": "string",
            "enum": ["normal", "reversed", "mixed"]
        }
    }
}

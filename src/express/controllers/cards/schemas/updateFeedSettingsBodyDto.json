{
  "type": "object",
  "required": ["card_orientation", "study_mode"],
  "additionalProperties": false,
  "properties": {
    "card_orientation": {
      "type": "string",
      "enum": ["normal", "reversed", "mixed"]
    },
    "study_mode": {
      "type": "string",
      "enum": ["write", "reveal"]
    }
  }
}
